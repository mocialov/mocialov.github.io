# CV Viewer – Public Assets

Place your exported CV HTML file here as `cv.html` to have it auto-loaded
when the app starts.

Example:
1. In the main LinkedIn app, click **💾 Export CV (HTML)**
2. Copy the downloaded `*_CV.html` file into this folder
3. Rename it to `cv.html`
4. Rebuild: `npm run build`
